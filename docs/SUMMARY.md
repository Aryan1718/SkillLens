# SkillLens Summary

This repository contains a minimal, runnable monorepo scaffold for SkillLens:
- `server/`: FastAPI API service
- `client/`: Vite + React + TypeScript UI
- `database/`: SQL schema stub

All modules are aligned with root `AGENTS.md` using server/client mapping.
